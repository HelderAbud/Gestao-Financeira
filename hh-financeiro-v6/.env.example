# Raiz do monorepo — copie para .env.local na Vercel ou use só como referência.
# Frontend (apps/web) em desenvolvimento:
NEXT_PUBLIC_API_URL=http://localhost:8090

# API (Render / Railway) — definir no painel do hosting, não commitar segredos:
# SPRING_DATASOURCE_URL=jdbc:postgresql://host:5432/db?sslmode=require
# SPRING_DATASOURCE_USERNAME=
# SPRING_DATASOURCE_PASSWORD=
# HH_JWT_SECRET=minimo-32-caracteres-aleatorios-seguros
# HH_CORS_ORIGINS=https://seu-app.vercel.app,http://localhost:3000
# OPENAI_API_KEY=sk-...   # opcional — ativa análise com modelo na API (/api/v1/insights/monthly-analysis)
# HH_OPENAI_MODEL=gpt-4o-mini
